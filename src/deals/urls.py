from django.urls import path

# from .views import (ProductListView, 
# 					ProductDetailView,
# 					)

urlpatterns = [
    # path('(?P<slug>[\w-]+)/$', ProductDetailView.as_view(), name='detail')
]